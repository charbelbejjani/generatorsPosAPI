meta {
  name: Get Zone By ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/zones/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Get a specific zone by ID.
  
  Change the ID in the URL as needed.
  
  Requires:
  - Authentication (valid access token)
  - Read permission on zones page
}
