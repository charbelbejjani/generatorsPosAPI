meta {
  name: Get Schedule By ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/schedules/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Get a specific payment schedule by ID.

  Change the ID in the URL as needed.

  Requires:
  - Authentication (valid access token)
  - Read permission on schedules page
}
