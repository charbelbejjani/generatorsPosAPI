meta {
  name: Get All Schedules
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/schedules
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Get all payment schedules ordered by year and month.

  Requires:
  - Authentication (valid access token)
  - Read permission on schedules page
}
