meta {
  name: Delete Schedule
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/schedules/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Delete a payment schedule by ID.

  Change the ID in the URL as needed.

  Requires:
  - Authentication (valid access token)
  - Delete permission on schedules page
}
