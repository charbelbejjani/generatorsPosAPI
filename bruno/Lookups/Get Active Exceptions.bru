meta {
  name: Get Active Exceptions
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/lookups/exceptions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Get all active exceptions for dropdown lists.
  
  Returns minimal data: id, name
  
  Requires:
  - Authentication (valid access token)
}
